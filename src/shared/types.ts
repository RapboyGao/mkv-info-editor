// 前后端通用类型定义

export interface Chapter {
  id: string;
  time: string;
  endTime: string;
  title: string;
  originalTitle: string;
  startTime: number;
  startTimeSeconds: number;
  endTimeSeconds: number;
}
